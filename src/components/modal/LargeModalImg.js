import { styled } from '@mui/material/styles';

const LargeModalImg = styled('img')(() => ({
  display: 'block',
  margin: '0 auto',
  objectFit: 'cover',
  height: '100%',
  width: '100%',
  cursor: 'zoom-out',
}));

export default LargeModalImg;
