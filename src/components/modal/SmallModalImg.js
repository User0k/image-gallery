import { styled } from '@mui/material/styles';

const SmallModalImg = styled('img')(() => ({
  display: 'block',
  margin: '0 auto',
  objectFit: 'contain',
  height: '40vw',
  width: 'auto',
  cursor: 'zoom-in',
}));

export default SmallModalImg;
